{
  "$schema": "http://json-schema.org/schema",
  "$id": "OverrideAppModule",
  "title": "Override app module",
  "type": "object",
  "description": "Override",
  "properties": {
    "name": {
      "type": "string",
      "x-prompt": "What is the name of the app that you want to override?"
    }
  },
  "required": ["name"]
}
